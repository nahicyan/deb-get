DEFVER=1
get_github_releases "mongodb-js/compass" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*mongodb-compass_.*${HOST_ARCH}\.deb\"" "${CACHE_FILE}" | cut -d '"' -f 4)
    VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL//v/}")
fi
PRETTY_NAME="MongoDB Compass"
WEBSITE="https://www.mongodb.com/products/compass"
SUMMARY="The MongoDB GUI"
